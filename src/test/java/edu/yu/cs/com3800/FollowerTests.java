package edu.yu.cs.com3800;

public class FollowerTests {

    // When the follower is started it must receive TCP messages from the TCPServer
    // queue. 
    // If they are work messages it must handle them by compiling the body
    // and returning the result
    // If they are not work messages they should be diregarded
    // If the leader dies while it is handling a work message it should store
    // that message till there is a new leader. Then it should send all the results
    // that it has computed to the new leader.


    
    
}
